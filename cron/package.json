{
  "name": "cron",
  "version": "1.0.0",
  "description": "```\r docker-compose run --rm app npm install",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node --require dotenv/config index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "moment": "^2.28.0",
    "promise-mysql": "^4.1.3"
  }
}
