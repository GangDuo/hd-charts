{
  "name": "cron",
  "version": "1.0.0",
  "description": "```\r docker-compose run --rm app npm install",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
